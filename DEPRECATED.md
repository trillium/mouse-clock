# Deprecated Code - To Remove When Ready

No deprecated code pending. All previously tracked items have been resolved:

- `adapter.py` debug overlay call — removed
- `adapter.py` debug print in draw() — removed
- `actions_move.py` debug prints in `_build_this_lines` — removed
- `actions_move.py` debug prints in `mouse_clock_this_shift` — removed
- `debug_overlay.py` — deleted (was unused after import removal)
